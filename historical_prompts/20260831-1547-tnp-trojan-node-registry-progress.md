## Exact user-message chain

Exact user input unavailable.

## AI-derived task summary

Implement a configuration-driven `trojan-node` registry so adding a node only requires a safe
OpenSSH destination, node ID, and domain. Add `node add/list/show`, local inventory fallback,
runtime credential placeholders, secret-safe deployment progress and heartbeats, bounded failure
recovery guidance, dynamic Mihomo ports, focused tests, and concise operator documentation. Keep
live Cloudflare/VPS actions outside this implementation task.
