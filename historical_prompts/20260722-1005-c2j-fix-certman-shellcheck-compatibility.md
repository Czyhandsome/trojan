# Historical prompt — chg-20260722-1005-c2j

Conceptual reconstruction: Fix the concrete ShellCheck findings from the failed GitHub Actions run with the smallest behavior-preserving patch, then rerun CI and merge only after it passes. The approved decision was to make argument passing and conditional control flow explicit and remove the duplicate adopt email confirmation.
