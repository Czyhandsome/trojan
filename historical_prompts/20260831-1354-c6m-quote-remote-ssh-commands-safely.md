## Exact user-message chain

Exact user input unavailable.

## AI-derived task summary

During the authorized Aiyun2 recovery preflight, diagnose and fix the local SSH
adapter bug that split a multi-line remote shell program. Keep live deployment
paused, preserve strict host verification, add a focused regression test, run
the existing release gates, and push the repaired exact branch commit before
retrying the read-only preflight.
