## Exact user-message chain

Exact user input unavailable. The implementation was governed by the explicit
physical plan archived with this change and the parent personal Task capsule.

## AI-derived task summary

Implement the offline-safe automation and tests first. Reuse only the user's
already established ED25519 alias trust, keep strict host checking, and use a
root-only exact-source provenance marker for managed reruns. Rotate both node
passwords and DNS tokens during fresh deployment, preserve least privilege,
deploy Aiyun2 first, and stop at the first failed gate.
