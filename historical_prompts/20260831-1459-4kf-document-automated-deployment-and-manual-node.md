## Exact user-message chain

Exact user input unavailable.

## AI-derived task summary

While the Aiyun2 acceptance soak continues, update `README.md` so a human can
deploy one declared Xray node through the guarded local CLI and can operate an
installed node without AI assistance. Include practical, secret-conscious
commands for retrieving the Trojan password, creating a Trojan URI and a
Clash/Mihomo YAML file, checking service and TLS health, handling certificates
and restarts, diagnosing connection failures, and refreshing SSH trust after a
server reinstall. Validate the copyable examples and push the exact docs change.
