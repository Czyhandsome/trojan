## Exact user-message chain

Exact user input unavailable.

## AI-derived task summary

Replace the overly strict exact Mihomo patch-version dependency with a minimum
supported-version contract that accepts the current Homebrew release. Preserve
fail-closed parsing, add focused tests and docs, run the full gates, and push
the exact change before resuming Aiyun2 client acceptance.
