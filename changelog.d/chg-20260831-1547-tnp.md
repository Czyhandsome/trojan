## 2026-08-31 chg-20260831-1547-tnp

Commit message: feat(node): add config-driven registry and deployment progress [chg-20260831-1547-tnp]

Summary: Register new Trojan nodes from an existing SSH destination and deploy them with
secret-safe progress, without adding per-node repository files.

Changes:

- Add local node inventory with legacy fallback and `node add/list/show` commands.
- Generate per-node creds placeholders at runtime and remove the two-node CLI restriction.
- Add ordered deployment stages, heartbeats, side-effect diagnostics, safe-resume guidance,
  reconnect progress, and dynamic Mihomo ports.
- Rework the README around the registration workflow and move machine operations to a runbook.

Verification:

- Passed: 73 Python CLI tests, 52 Bats, ShellCheck, bash parse, Ruff, Mypy,
  Python bytecode compilation, `git diff --check`, JSON parsing, and production-surface
  secret-marker scan.
- Passed: Trojan URI example with reserved characters.
- Not available on this macOS host: `systemd-analyze verify`; the unchanged unit suite remains
  covered by Bats and the Linux CI job.
- Not run: real node registration, Cloudflare/VPS writes, credential rotation, or deployment.
