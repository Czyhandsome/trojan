## 2026-08-31 chg-20260831-1354-c6m

Commit message: fix(ssh): quote remote command argv safely [chg-20260831-1354-c6m]

Summary: Preserve remote argv boundaries when the strict SSH adapter sends
multi-line shell programs, fixing the first live Aiyun2 preflight failure without
changing the installer or any Cloudflare and VPS state transitions.

Changes:

- Join the complete remote argv with Python's standard shell quoting before
  handing it to OpenSSH, whose remote command transport is a single shell string.
- Add a focused regression test that proves a multi-line `sh -c` program remains
  one remote command argument.

Verification:

- Passed: focused SSH red/green slice; Trojan CLI 54 tests with
  `ResourceWarning` treated as error; existing certman 52 Bats; ShellCheck;
  bash parse; Python compile; Ruff; Mypy; `git diff --check`; production-surface
  secret-marker scan.
- Passed read-only: Cloudflare manager token sees the exact zone; Aiyun2 strict
  BatchMode SSH and trusted ED25519 alias check.
- Failed before this fix: Aiyun2 remote preflight response parsing because the
  multi-line shell script was not kept as one `sh -c` argument.
- Not run after this fix: Aiyun2 preflight, credential rotation, DNS mutation,
  VPS deployment, Mihomo/GCP acceptance, or Clash modification.
