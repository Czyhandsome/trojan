## 2026-08-31 chg-20260831-1131-tnr

Commit message: feat(node): add guarded dual-node recovery CLI [chg-20260831-1131-tnr]

Summary: Add the local, Trojan-scoped orchestration needed to rebuild the two
clean Ubuntu 24.04 Xray nodes without copying secrets through chat, argv,
persistent installer input files, or the user's global SSH trust store.

Changes:

- Add `bin/trojan-node`, a Python standard-library CLI with exact one-node
  command routing, redacted plan/apply confirmation, strict ED25519 trust,
  exact-source staging, in-memory secret transport, full server verification,
  and isolated Mihomo/GCP acceptance.
- Add a non-secret two-node manifest and per-scope `creds` placeholder files.
  SSH reuses the user's already established ED25519 alias trust through a
  dedicated temporary known_hosts file; it never promotes keyscan output.
- Add an atomic root-only provenance marker bound to node identity and exact
  source archive so a later identical deploy can prove ownership before rerun.
- Add bounded Cloudflare zone/DNS/token APIs with pagination, dynamic minimum
  permission resolution, exact-zone and `/32` token policy, conflict refusal,
  write compensation, and 30-day manager-token expiry warning.
- Add offline tests, CI coverage, secret-marker scans, and operator docs. The
  existing remote transactional installer remains unchanged.

Verification:

- Passed: behavior-specific red/green slices; Trojan CLI `53 tests` with
  `ResourceWarning` treated as error; existing certman `52 Bats`; ShellCheck;
  bash parse; Python compile; CLI help; `git diff --check`.
- Pending: repository branch closeout/CI, canonical creds merge and reinstall,
  Cloudflare manager token,
  Aiyun2/Aiyun1 live deployment, Mihomo soak, and 24-hour observation.
- Passed read-only: strict BatchMode SSH through the user's existing ED25519
  alias trust for both nodes.
- Not run: any real Keychain write, Cloudflare mutation,
  VPS change, Mihomo process, GCP reconnect, or Clash modification.

Known gap:

- A managed rerun is intentionally limited to the exact source archive and
  existing node credentials; it cannot combine with `--rotate-secrets`.
