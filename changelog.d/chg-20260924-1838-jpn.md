## 2026-09-24 chg-20260924-1838-jpn

Commit message: feat(clash): add JPP-SSH Japan SOCKS5 node to default profile [chg-20260924-1838-jpn]

Summary: Add the newly provisioned Japan SSH tunnel exit as a second client-only SOCKS5 node in the default personal Clash profile, verified as an OpenAI-capable exit, without changing generator capabilities.

Changes:

- Add `JPP-SSH` at `127.0.0.1:1089` to `config/clash-profile.json` after `HKP-SSH`; default `Aiyun1`, existing nodes and rules unchanged.
- Extend Clash profile regression assertions to cover the second SOCKS5 node in spec order, rendered output and CLI stdout.
- Document `JPP-SSH` usage and record that the Japan exit answers OpenAI API with a normal authentication error instead of a region rejection, so it can serve as the OpenAI split target.

Verification:

- Passed: direct SSH `-D` tunnel to the new 50122 endpoint; exit `35.78.250.198` at Tokyo/JP, Google HTTP 200, OpenAI API returned `Missing bearer authentication` (no `unsupported_country_region_territory`).
- Passed: Python CLI regression tests, `git diff --check`, Python compilation, and Mihomo config validation.
- Passed: isolated generated-profile canary with synthetic passwords; selected `JPP-SSH` through a standalone Mihomo process, observed the Tokyo exit and OpenAI auth-only error; canary stopped only its owned SSH/Mihomo processes.
- Not performed: existing Clash Verge activation, system proxy/TUN changes, server deployment, or remote node changes.
