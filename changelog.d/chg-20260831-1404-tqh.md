## 2026-08-31 chg-20260831-1404-tqh

Commit message: fix(cloudflare): use single-page permission groups API [chg-20260831-1404-tqh]

Summary: Follow Cloudflare's current single-page permission-groups contract so
the guarded node-token rotation can resolve the exact Zone Read and DNS Write
IDs without sending unsupported pagination parameters.

Changes:

- Replace the generic paginated request for
  `/user/tokens/permission_groups` with one strict single-page GET.
- Validate that the returned result is an array of objects before resolving the
  two exact minimum permission groups.
- Add a focused regression test that rejects use of the pagination adapter.

Verification:

- Passed: focused red/green slice; Trojan CLI 55 tests with `ResourceWarning`
  treated as error; existing certman 52 Bats; ShellCheck; bash parse; Python
  compile; Ruff; Mypy; `git diff --check`; production-surface secret scan.
- Passed live read-only: manager token can list the exact zone and user tokens;
  Aiyun2 remains clean Ubuntu 24.04 with 443 free and no managed state.
- Failed before this fix: Cloudflare rejected the paginated GET to the
  single-page permission-groups endpoint.
- Not run after this fix: token creation, DNS mutation, VPS deployment,
  Mihomo/GCP acceptance, or Clash modification.
