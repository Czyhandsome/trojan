## 2026-09-24 chg-20260924-1127-csn

Commit message: feat(clash): support custom SOCKS5 nodes [chg-20260924-1127-csn]

Summary: Allow the existing personal Clash profile generator to include strict, unauthenticated SOCKS5 client nodes such as the local HKP SSH tunnel without expanding server inventory or credential handling.

Changes:

- Accept `{name, type: socks5, server, port}` client-only entries in the profile spec with strict host/port and reserved-name validation.
- Render SOCKS5 entries into the existing selectable group with `udp: false`; keep Trojan password, TLS, and Keychain behavior unchanged.
- Add the local `HKP-SSH` example at `127.0.0.1:1088` and document that the generator does not start or verify the tunnel.

Verification:

- Passed: 98 Python CLI tests, including 25 Clash profile tests, `git diff --check`, Python compilation, and Mihomo config validation.
- Passed: isolated generated-profile canary with synthetic passwords; selected `HKP-SSH`, observed exit `47.76.197.91` at HKG, and fetched Google HTTP 200; restored default `Aiyun1`.
- Passed: canary used no real credentials and stopped only its owned SSH/Mihomo processes; existing system proxy and routes were not changed.
- Not performed: existing Clash Verge activation, system proxy/TUN changes, server deployment, or remote node changes.
