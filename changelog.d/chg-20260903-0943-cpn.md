## 2026-09-03 chg-20260903-0943-cpn

Commit message: feat(clash): render a selectable personal node profile [chg-20260903-0943-cpn]

Summary: Generate one Mihomo-validated local Clash profile from a tracked, editable rules template
and three Keychain-backed personal node passwords without committing or printing credentials.

Changes:

- Add a non-secret profile spec that references the managed Aiyun nodes and declares the
  client-only Solo-green endpoint.
- Add the current lightweight routing rules as an editable template with controlled insertion
  markers for generated proxies and group members.
- Add `trojan-node clash render --output FILE` with password-only credential injection, strict
  profile validation, secret-safe diagnostics, 0600 temporary output and atomic no-overwrite
  installation.
- Document Keychain setup, safe rendering, Clash Verge import, temporary-file cleanup and later
  rules-template updates.

Verification:

- Passed: 89 Python CLI tests, 52 Bats, Ruff, CLI Mypy, ShellCheck, Python bytecode compilation,
  JSON/template checks, production-surface secret scan and `git diff --check`.
- Passed: a real Keychain-backed render produced a 0600 profile that local Mihomo 1.19.30
  validated against Clash Verge's existing geodata directory.
- Passed: the user confirmed Clash Verge imported the generated profile; an isolated loopback-only
  Mihomo process read back the default and ordered group members, then each of Aiyun1, Aiyun2 and
  Solo-green returned HTTP 204 before Aiyun1 was restored.
- Not performed: Clash Verge activation, system proxy/TUN/routing changes, old local-profile
  deletion, paid remote-profile refresh or any VPS, Cloudflare, DNS, certificate or server-side
  operation.
